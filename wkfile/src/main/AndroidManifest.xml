<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity android:name=".ChooseFileActivity" />
        <activity android:name=".ChatFileActivity" />
        <activity android:name=".SearchChatFileActivity" />
    </application>
</manifest>
